# CManga Anime4K 超分辨率集成记录（含网络图片支持）

> 历史技术记录：本文件保留原始 v1 集成的设计和接口说明，不代表当前原生 AI 管线的完整行为。当前 CManga 的模型、缓存、平台要求和构建命令请以 [README](README.md) 为准。

## 修改概览

CManga 保留了最初从开源阅读器代码集成的 Anime4K v1 超分辨率功能，**同时支持本地图片和网络图片**。以下记录当时涉及的 **7 个文件**（2 个新建，5 个修改）。

---

## 新建文件

### 1. `lib/utils/anime4k/anime4k_upscaler.dart`

**Anime4K 核心算法实现**，基于 Anime4K v1.0 "Push Pixels" 算法，纯 Dart 实现，无需 Native 依赖。

**算法流程：**

| 步骤 | 操作 | 说明 |
|------|------|------|
| 1 | 双线性插值放大 | 将图像放大到目标分辨率 |
| 2 | 计算亮度图 | 用于后续梯度和细化计算 |
| 3 | 线条细化（Unblur） | 将暗像素推向亮区域，细化动漫线条 |
| 4 | Sobel 梯度计算 | 检测图像边缘 |
| 5 | 梯度精炼 | 利用梯度信息将像素推向边缘，锐化细节 |

> 所有计算在 `compute()` 中执行，不阻塞 UI 线程。

---

### 2. `lib/utils/anime4k/anime4k_service.dart`

**Anime4K 服务层**，提供文件缓存、防重复处理、本地文件处理和字节数据处理等功能。

**主要功能：**
- 单例模式（`Anime4KService.instance`）
- 基于文件路径/URL + 参数组合的磁盘缓存（存储于临时目录 `anime4k_cache/`）
- 防止同一图片重复处理的并发控制
- `processFile()` — 处理本地图片文件
- `processImage()` — 处理任意字节数据（供网络图片使用）
- 支持清除缓存和查询缓存大小
- **日志修复**：已适配 CManga 使用的 `Log.info()` 和 `Log.error()` 调用方式。

---

## 修改文件

### 3. `lib/foundation/appdata.dart`

在 `Settings` 类中新增 **5 个** Anime4K 配置字段，确保配置可持久化并支持跨设备同步（如未加入 `_disableSync`）。

| 字段 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `enableAnime4K` | `bool` | `false` | 是否对本地图片启用超分 |
| `anime4KScaleFactor` | `double` | `2.0` | 放大倍数 |
| `anime4KPushStrength` | `double` | `0.31` | 线条细化强度 |
| `anime4KPushGradStrength` | `double` | `1.0` | 梯度精炼强度 |
| `enableAnime4KForNetwork` | `bool` | `false` | **新增** 是否对网络图片也启用超分 |

---

### 4. `lib/pages/settings/reader.dart`

在阅读设置页面更新 **Anime4K 超分辨率** 设置区块，新增网络图片超分开关。

| 控件 | 说明 |
|------|------|
| **启用 Anime4K 超分辨率** | 主开关，控制本地图片超分（`enableAnime4K`） |
| **网络图片也进行超分** *(新增)* | 子开关，仅在主开关开启时显示，控制在线浏览时的网络图片超分（`enableAnime4KForNetwork`） |
| **放大倍数** | 滑块或选择器，选项：1.5x / 2.0x / 3.0x / 4.0x |
| **线条细化强度** | 滑块或选择器，选项：0.00 / 0.15 / 0.31 / 0.50 / 0.75 |
| **梯度精炼强度** | 滑块或选择器，选项：0.00 / 0.50 / 1.00 / 1.50 / 2.00 |

---

### 5. `lib/pages/reader/comic_image.dart`

集成完整的超分处理逻辑，新增方法并替换图片加载调用。

**新增成员变量：**
- `_upscaledBytes` — 缓存超分结果的内存数据
- `_isUpscaling` — 标记当前是否正在进行超分处理

**核心逻辑：**

| 方法 | 说明 |
|------|------|
| `_triggerImageUpscale()` | 超分触发器：根据配置判断是否需要超分。对于本地文件或已加载的网络图片字节，调用 `Anime4KService` 进行处理。 |
| `build()` | 优先判断 `_upscaledBytes` 是否有值，若有则使用 `RawImage` 渲染超分后的内存数据，否则渲染原始图片。 |

**网络图片超分工作流程：**

```
图片加载完成
    ↓
_triggerImageUpscale() 被调用
    ↓
从 ReaderImageProvider 获取原始字节数据
    ↓
Anime4KService.processImage() 在 Isolate 中处理
    ↓
setState(() { _upscaledBytes = result; })
    ↓
Widget 重建，显示超分后的图像
```

---

### 6. `lib/init.dart`

在 `init()` 方法中添加 `Anime4KService.instance.init()` 调用，确保应用启动时初始化超分缓存目录。

---

## 使用说明

1. 进入 **设置** -> **阅读设置** 页面
2. 找到 **Anime4K 超分辨率** 区块
3. 打开 **启用 Anime4K 超分辨率** 开关（本地图片超分）
4. 如需对在线浏览的网络图片也超分，打开 **网络图片也进行超分** 开关
5. 根据需要调整放大倍数、线条细化强度和梯度精炼强度

> **注意：** 网络图片超分在图片加载完成后异步触发，处理期间显示原始图片，完成后无缝切换为超分结果。超分结果会被缓存，相同图片+参数组合只处理一次。

---

## 性能建议

| 设备性能 | 推荐放大倍数 | 网络图片超分 |
|----------|-------------|-------------|
| 高端设备 | 3.0x ~ 4.0x | 可开启 |
| 中端设备 | 2.0x | 谨慎开启（会增加内存和处理时间） |
| 低端设备 | 1.5x | 建议关闭（仅开启本地图片超分） |
