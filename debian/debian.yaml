flutter_app: 
  command: cmanga
  arch: {{Arch}}
  parent: /usr/local/lib
  nonInteractive: true

control:
  Package: cmanga
  Version: {{Version}}
  Architecture: {{Architecture}}
  Priority: optional
  Depends: libwebkit2gtk-4.1-0, libgtk-3-0
  Maintainer: nyne
  Description: CManga comic reader for local and network libraries
  
#options:
#  exec_out_dir: debian/packages